# Candidate Changes

Each candidate changes one target layer only and must be sanitized. Candidates
are shadow-only; they cannot change active rules, boot files, or scripts.

Suggested filename: `YYYY-MM-DD_<candidate-id>.yaml`.
